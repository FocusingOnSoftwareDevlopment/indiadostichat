document.addEventListener('DOMContentLoaded', () => { const mobileMenuBtn = document.querySelector('.mobile-menu-btn'); const navLinks = document.querySelector('.nav-links'); if (mobileMenuBtn) { mobileMenuBtn.addEventListener('click', () => { navLinks.classList.toggle('active'); const icon = mobileMenuBtn.querySelector('i'); if (navLinks.classList.contains('active')) { icon.classList.remove('fa-bars'); icon.classList.add('fa-times'); } else { icon.classList.remove('fa-times'); icon.classList.add('fa-bars'); } }); } const chatGate = document.getElementById('chatGate'); const chatWrapper = document.getElementById('chatWrapper'); const enterChatBtn = document.getElementById('enterChatBtn'); if (chatGate && enterChatBtn) { enterChatBtn.addEventListener('click', () => { enterChatBtn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Verifying...'; enterChatBtn.disabled = true; setTimeout(() => { chatGate.style.display = 'none'; chatWrapper.style.display = 'block'; const header = document.querySelector('header'); if (header) header.style.display = 'none'; const main = document.querySelector('main'); if (main) { main.style.marginTop = '0'; main.style.height = '100vh'; } const container = document.querySelector('.chat-container'); if (container) { container.style.height = '100vh'; } }, 1000); }); } const currentLocation = location.pathname.split('/').pop(); const navItems = document.querySelectorAll('.nav-links a'); navItems.forEach(item => { const href = item.getAttribute('href'); if (href === currentLocation || (currentLocation === '' && href === 'index.html')) { item.style.color = 'var(--primary-color)'; } }); }); function onTurnstileSuccess(token) { const enterChatBtn = document.getElementById('enterChatBtn'); if (enterChatBtn) { enterChatBtn.disabled = false; enterChatBtn.textContent = 'Join Room #IndiaDostiChat'; } } document.addEventListener('DOMContentLoaded', () => { const visitorCountEls = document.querySelectorAll('#visitor-count'); if (visitorCountEls.length > 0) { fetch('https: .then(response => response.json()) .then(data => { visitorCountEls.forEach(el => { el.innerText = data.count; }); }) .catch(error => { console.error('Error fetching visitor count:', error); visitorCountEls.forEach(el => { el.innerText = 'Unavailable'; }); }); } const themeToggle = document.getElementById("theme-toggle"); const themeIcon = themeToggle ? themeToggle.querySelector(".theme-icon") : null; function applyTheme(theme) { const isDark = theme === "dark"; document.body.classList.toggle("dark-mode", isDark); if (themeIcon) { themeIcon.textContent = isDark ? "☀️" : "🌙"; } if (themeToggle) { themeToggle.setAttribute("aria-label", isDark ? "Switch to light mode" : "Switch to dark mode"); themeToggle.setAttribute("title", isDark ? "Switch to light mode" : "Switch to dark mode"); } } let savedTheme = null; try { savedTheme = localStorage.getItem("theme"); } catch (e) { console.warn("localStorage not available", e); } const systemPrefersDark = window.matchMedia && window.matchMedia("(prefers-color-scheme: dark)").matches; const initialTheme = savedTheme || (systemPrefersDark ? "dark" : "light"); applyTheme(initialTheme); if (themeToggle) { themeToggle.addEventListener("click", function (e) { e.preventDefault(); const isDark = document.body.classList.contains("dark-mode"); const newTheme = isDark ? "light" : "dark"; try { localStorage.setItem("theme", newTheme); } catch (err) {} applyTheme(newTheme); }); } const copyButtons = document.querySelectorAll('.copy-invite-btn'); copyButtons.forEach(btn => { btn.addEventListener('click', () => { const inviteText = btn.getAttribute('data-invite'); if (inviteText && navigator.clipboard) { navigator.clipboard.writeText(inviteText).then(() => { const originalText = btn.textContent; btn.textContent = 'Copied!'; btn.style.backgroundColor = 'var(--secondary-color)'; btn.style.color = '#ffffff'; setTimeout(() => { btn.textContent = originalText; btn.style.backgroundColor = ''; btn.style.color = ''; }, 2000); }).catch(err => { console.error('Could not copy text: ', err); }); } }); }); const landingHeroes = document.querySelectorAll('.landing-hero'); const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches; if (!prefersReducedMotion && landingHeroes.length > 0) { landingHeroes.forEach(hero => { const bgAttr = hero.getAttribute('data-bg-images'); if (bgAttr) { const images = bgAttr.split(',').map(src => src.trim()).filter(src => src.length > 0); if (images.length > 1) { let currentIndex = 0; images.forEach(src => { const img = new Image(); img.src = src; }); hero.style.setProperty('--landing-bg-image', `url('${images[currentIndex]}')`); setInterval(() => { currentIndex = (currentIndex + 1) % images.length; hero.style.setProperty('--landing-bg-image', `url('${images[currentIndex]}')`); }, 8000); } else if (images.length === 1) { hero.style.setProperty('--landing-bg-image', `url('${images[0]}')`); } } }); } }); 